<Page x:Class="LasanthaPOS.Desktop.Views.CustomersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Customers" Background="#F5F5F5">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Customer Management" FontSize="24"
                   FontWeight="Bold" Foreground="#1976D2" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox x:Name="TxtSearch" Width="280" Height="36" Padding="8,6" FontSize="14"
                     Margin="0,0,8,0"
                     KeyUp="TxtSearch_KeyUp"/>
            <Button Content="Search" Style="{StaticResource PrimaryButton}" Height="36" Margin="0,0,8,0" Click="BtnSearch_Click"/>
            <Button Content="Load All" Style="{StaticResource PrimaryButton}" Height="36" Margin="0,0,8,0" Click="BtnLoadAll_Click"/>
            <Button Content="+ Add Customer" Style="{StaticResource PrimaryButton}" Height="36" Click="BtnAdd_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="DgCustomers" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single"
                  GridLinesVisibility="Horizontal" BorderThickness="1" BorderBrush="#E0E0E0"
                  Background="White" RowBackground="White" AlternatingRowBackground="#F9F9F9"
                  FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="130"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Loyalty Card" Binding="{Binding LoyaltyCardId}" Width="130"/>
                <DataGridTextColumn Header="Points" Binding="{Binding LoyaltyPoints}" Width="80"/>
                <DataGridTemplateColumn Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Edit" Padding="6,2" FontSize="11"
                                    Background="#1976D2" Foreground="White" BorderThickness="0"
                                    Click="BtnEdit_Click" Tag="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
