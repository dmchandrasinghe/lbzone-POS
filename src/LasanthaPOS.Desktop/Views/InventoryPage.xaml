<Page x:Class="LasanthaPOS.Desktop.Views.InventoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Inventory" Background="#F5F5F5">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Inventory Management" FontSize="24"
                   FontWeight="Bold" Foreground="#1976D2" Margin="0,0,0,16"/>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox x:Name="TxtSearch" Width="300" Height="36" Padding="8,6" FontSize="14"
                     Margin="0,0,8,0"
                     KeyUp="TxtSearch_KeyUp"/>
            <Button Content="Search" Style="{StaticResource PrimaryButton}" Height="36" Margin="0,0,8,0" Click="BtnSearch_Click"/>
            <Button Content="Load All" Style="{StaticResource PrimaryButton}" Height="36" Margin="0,0,8,0" Click="BtnLoadAll_Click"/>
            <Button Content="⚠ Low Stock" Height="36" Background="#FF8F00" Foreground="White"
                    BorderThickness="0" Padding="12,4" Margin="0,0,8,0" Click="BtnLowStock_Click" Cursor="Hand"/>
            <Button Content="+ Add Product" Style="{StaticResource PrimaryButton}" Height="36" Click="BtnAdd_Click"/>
        </StackPanel>

        <!-- Product Grid -->
        <DataGrid Grid.Row="2" x:Name="DgProducts" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single"
                  GridLinesVisibility="Horizontal" BorderThickness="1" BorderBrush="#E0E0E0"
                  Background="White" RowBackground="White" AlternatingRowBackground="#F9F9F9"
                  FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item Code" Binding="{Binding ItemCode}" Width="110"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="110"/>
                <DataGridTextColumn Header="Supplier" Binding="{Binding Supplier}" Width="130"/>
                <DataGridTextColumn Header="Buy Price" Binding="{Binding BuyingPrice, StringFormat=\{0:F2\}}" Width="90"/>
                <DataGridTextColumn Header="Sell Price" Binding="{Binding SellingPrice, StringFormat=\{0:F2\}}" Width="90"/>
                <DataGridTextColumn Header="Margin %" Binding="{Binding ProfitMargin, StringFormat=\{0:F1\}}" Width="80"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="60"/>
                <DataGridTextColumn Header="Total Cost" Binding="{Binding TotalCost, StringFormat=\{0:F2\}}" Width="90"/>
                <DataGridTextColumn Header="Warranty" Binding="{Binding WarrantyMonths}" Width="80"/>
                <DataGridTemplateColumn Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Margin="2" Padding="6,2" FontSize="11"
                                        Background="#1976D2" Foreground="White" BorderThickness="0"
                                        Click="BtnEdit_Click" Tag="{Binding}"/>
                                <Button Content="Del" Margin="2" Padding="6,2" FontSize="11"
                                        Background="#C62828" Foreground="White" BorderThickness="0"
                                        Click="BtnDelete_Click" Tag="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock x:Name="TxtStatus" Grid.Row="3" Margin="0,8,0,0" FontSize="12" Foreground="#607D8B"/>
    </Grid>
</Page>
